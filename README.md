# Step_semester_3

## Session 6: Library Membership

Compile with `javac *.java` and run `java Session6MembershipDemo`.

`LibraryMember` and its subclasses cover validated batch enrollment, multilevel and hierarchical inheritance, polymorphic reports, discounted fine history, immutable member numbers, renewal-code checks, overloaded borrowing, and null-safe nightly audits.